// The validator moved to @receivy/common so inline Pix keys on billings share it; this re-export keeps the old import path.

export type { PixKeyType } from '@receivy/common';
export { normalizePixKey } from '@receivy/common';
