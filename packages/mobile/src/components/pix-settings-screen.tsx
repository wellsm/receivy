import type { PaymentMethod, PaymentMethodInput, PixKeyType } from "@receivy/common";
import { useEffect, useState } from "react";
import { ActivityIndicator, Pressable, ScrollView, Text, TextInput, View } from "react-native";
import { SafeAreaView } from "@/components/safe-area-view";
import { financialClient, type FinancialClient } from "@/financial/client";

export function PixSettingsScreen({ client = financialClient, onBack }: { client?: Pick<FinancialClient, "paymentMethods" | "savePaymentMethod" | "defaultPaymentMethod" | "archivePaymentMethod">; onBack?: () => void }) {
  const [items, setItems] = useState<PaymentMethod[]>([]); const [type, setType] = useState<PixKeyType>("email"); const [key, setKey] = useState(""); const [label, setLabel] = useState(""); const [editing, setEditing] = useState<string>(); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState("");
  useEffect(() => { void client.paymentMethods().then(page => setItems(page.paymentMethods)).catch(reason => setError(reason instanceof Error ? reason.message : "Não foi possível carregar suas chaves Pix.")); }, [client]);
  async function perform(action: () => Promise<unknown>, staleMessage = "Alteração salva, mas não foi possível atualizar a lista agora.") { setBusy(true); setError(""); setNotice(""); try { await action(); } catch (reason) { setError(reason instanceof Error ? reason.message : "Não foi possível atualizar a chave Pix."); setBusy(false); return false; } try { setItems((await client.paymentMethods()).paymentMethods); setNotice("Chaves Pix atualizadas."); } catch { setNotice(staleMessage); } finally { setBusy(false); } return true; }
  async function save() { const input: PaymentMethodInput = { pixKeyType: type, pixKey: key, label: label || undefined }; const saved = await perform(() => client.savePaymentMethod(input, editing), "Chave salva, mas não foi possível atualizar a lista agora."); if (saved) { setEditing(undefined); setKey(""); setLabel(""); } }
  return <SafeAreaView className="flex-1 bg-canvas"><ScrollView keyboardShouldPersistTaps="handled" contentContainerClassName="gap-5 px-5 pb-12 pt-2"><Pressable accessibilityRole="button" onPress={onBack} className="min-h-12 justify-center"><Text className="font-bold text-primary">← Timeline</Text></Pressable><Text className="text-4xl font-extrabold text-primary-strong">Suas chaves Pix</Text><Text className="leading-6 text-muted">O Receivy apenas exibe a chave nos links. O pagamento acontece no banco.</Text><View className="gap-3 rounded-3xl border border-outline bg-surface p-5"><Text className="text-xl font-bold text-primary-strong">{editing ? "Editar chave" : "Adicionar chave"}</Text><ScrollView horizontal contentContainerClassName="gap-2">{(["cpf", "cnpj", "email", "phone", "random"] as PixKeyType[]).map(value => <Pressable key={value} accessibilityRole="button" onPress={() => setType(value)} className={`min-h-12 justify-center rounded-full border px-4 ${type === value ? "bg-primary-soft" : "border-outline"}`}><Text className="font-bold text-primary-strong">{value.toUpperCase()}</Text></Pressable>)}</ScrollView><TextInput accessibilityLabel="Chave Pix" placeholder="Chave Pix" value={key} onChangeText={setKey} className="min-h-12 rounded-xl border border-outline px-3 text-ink" /><TextInput accessibilityLabel="Nome da chave" placeholder="Nome para identificar" value={label} onChangeText={setLabel} className="min-h-12 rounded-xl border border-outline px-3 text-ink" /><Pressable accessibilityRole="button" accessibilityLabel="Salvar chave" disabled={busy || !key.trim()} onPress={() => void save()} className="min-h-12 items-center justify-center rounded-xl bg-primary">{busy ? <ActivityIndicator color="white" /> : <Text className="font-bold text-white">Salvar chave</Text>}</Pressable></View><View className="gap-3">{items.map(item => <View key={item.id} className="gap-3 rounded-2xl border border-outline bg-surface p-4"><Text className="text-lg font-bold text-ink">{item.label || item.pixKeyType.toUpperCase()} {item.isDefault ? "· Principal" : ""}</Text><Text selectable className="text-muted">{item.pixKey}</Text><View className="flex-row flex-wrap gap-4"><Pressable accessibilityRole="button" onPress={() => { setEditing(item.id); setType(item.pixKeyType); setKey(item.pixKey); setLabel(item.label); }} className="min-h-12 justify-center"><Text className="font-bold text-primary">Editar</Text></Pressable>{!item.isDefault && <Pressable accessibilityRole="button" disabled={busy} onPress={() => void perform(() => client.defaultPaymentMethod(item.id))} className="min-h-12 justify-center"><Text className="font-bold text-primary">Tornar principal</Text></Pressable>}<Pressable accessibilityRole="button" disabled={busy} onPress={() => void perform(() => client.archivePaymentMethod(item.id))} className="min-h-12 justify-center"><Text className="text-red-700">Arquivar</Text></Pressable></View></View>)}</View>{error ? <Text accessibilityRole="alert" className="rounded-xl bg-red-50 p-4 text-red-700">{error}</Text> : null}{notice ? <Text accessibilityRole="alert" className="rounded-xl bg-primary-soft p-4 text-primary-strong">{notice}</Text> : null}</ScrollView></SafeAreaView>;
}
